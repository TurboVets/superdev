# Lessons ledger

Staging area for operational lessons (rubric LE-1). Each entry is the rule first, the archaeology second, ≤6 lines. When a lesson hardens, promote it into `SKILL.md` **replacing prose, not adding**.

Start empty. Do not import another operator's ledger.
